<div class="container">
  <div class="row">
    <div class="col-lg-5">
      <br><br><br>
      <div id="word-cloud" data-id="<%= @state.id %>" style="width: 550px; height: 350px;"></div>
    </div>
    <div class="col-lg-7">
      <h1>Here is where sentiment analyses go</h1>
      <ul>
        <%= @state.words.each do |word| %>
          <li><%= word.word %>: POS <%= word.percent_positive %>%, NEG <%= word.percent_negative %>%</li>
          <li>
            <% word.random_quotes.each do |quote| %>
              "<%= quote.snippet %>" - <%= quote.speaker %><br>
            <% end %>
          </li>
          <br><br>
        <% end  %>
      </ul>
    </div>
  </div>
</div>
